{
  "name": "library-rules-test",
  "private": true,
  "description": "Tests für firestore.rules im Emulator (siehe rules.test.js)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-library \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.0",
    "firebase-tools": "^14.0.0"
  }
}
